defaults: &defaults
  adapter: mysql2
  host: 127.0.0.1
  pool: 25
  username: root
  password: 

development:
  <<: *defaults
  database: refinery_dev

test: &test
  <<: *defaults
  database: refinery_test

production:
  uri: <%= ENV['DATABASE_URL'] %>